import { MonaSplitButtonMessages } from "@nanahoshi/mona-ui/i18n";

export const SPLIT_BUTTON_DEFAULT_MESSAGES: MonaSplitButtonMessages = {
    menuButtonAriaLabel: "Show menu options",
    splitButton: (text: string) => (text ? `${text} splitbutton` : "Split button")
};

