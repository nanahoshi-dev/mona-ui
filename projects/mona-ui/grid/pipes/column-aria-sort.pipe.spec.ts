import { ColumnAriaSortPipe } from "./column-aria-sort.pipe";

describe("ColumnAriaSortPipe", () => {
    it("create an instance", () => {
        const pipe = new ColumnAriaSortPipe();
        expect(pipe).toBeTruthy();
    });
});
