import { AttributeBinderDirective } from './attribute-binder.directive';

describe('AttributeBinderDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeBinderDirective();
    expect(directive).toBeTruthy();
  });
});
