import { ResizePositionPipe } from './resize-position.pipe';

describe('ResizePositionPipe', () => {
  it('create an instance', () => {
    const pipe = new ResizePositionPipe();
    expect(pipe).toBeTruthy();
  });
});
