import { MonthViewDayDirective } from './month-view-day.directive';

describe('MonthViewDayDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthViewDayDirective();
    expect(directive).toBeTruthy();
  });
});
