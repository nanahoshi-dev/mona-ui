import { GridRowStylePipe } from "./grid-row-style.pipe";

describe("GridRowStylePipe", () => {
    it("create an instance", () => {
        const pipe = new GridRowStylePipe();
        expect(pipe).toBeTruthy();
    });
});
