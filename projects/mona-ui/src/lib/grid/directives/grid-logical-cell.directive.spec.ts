import { GridLogicalCellDirective } from "./grid-logical-cell.directive";

describe("GridLogicalCellDirective", () => {
    it("should create an instance", () => {
        const directive = new GridLogicalCellDirective();
        expect(directive).toBeTruthy();
    });
});
