import { GridFilterableDirective } from "./grid-filterable.directive";

describe("GridFilterableDirective", () => {
    it("should create an instance", () => {
        const directive = new GridFilterableDirective();
        expect(directive).toBeTruthy();
    });
});
