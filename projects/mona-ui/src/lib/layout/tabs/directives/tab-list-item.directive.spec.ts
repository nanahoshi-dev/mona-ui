import { TabListItemDirective } from './tab-list-item.directive';

describe('TabListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TabListItemDirective();
    expect(directive).toBeTruthy();
  });
});
