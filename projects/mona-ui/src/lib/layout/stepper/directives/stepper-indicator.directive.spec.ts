import { StepperIndicatorDirective } from "./stepper-indicator.directive";

describe("StepperIndicatorDirective", () => {
    it("should create an instance", () => {
        const directive = new StepperIndicatorDirective();
        expect(directive).toBeTruthy();
    });
});
