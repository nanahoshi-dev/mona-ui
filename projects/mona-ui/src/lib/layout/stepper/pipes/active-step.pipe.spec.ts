import { ActiveStepPipe } from './active-step.pipe';

describe('ActiveStepPipe', () => {
    it('create an instance', () => {
        const pipe = new ActiveStepPipe();
        expect(pipe).toBeTruthy();
    });
});
