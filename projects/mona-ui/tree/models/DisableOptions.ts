export interface DisableOptions {
    /**
     * @description Determines whether the children of the node should be disabled as well.
     */
    disableChildren: boolean;

    /**
     * @description Determines if the disabling is enabled.
     */
    enabled: boolean;
}
