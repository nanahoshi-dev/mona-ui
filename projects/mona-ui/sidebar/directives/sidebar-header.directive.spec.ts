import { SidebarHeaderDirective } from "./sidebar-header.directive";

describe("SidebarHeaderDirective", () => {
    it("should create an instance", () => {
        const directive = new SidebarHeaderDirective();
        expect(directive).toBeTruthy();
    });
});
