import { SidebarMenuDirective } from "./sidebar-menu.directive";

describe("SidebarMenuDirective", () => {
    it("should create an instance", () => {
        const directive = new SidebarMenuDirective();
        expect(directive).toBeTruthy();
    });
});
