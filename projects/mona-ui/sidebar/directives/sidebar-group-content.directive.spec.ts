import { SidebarGroupContentDirective } from "./sidebar-group-content.directive";

describe("SidebarGroupContentDirective", () => {
    it("should create an instance", () => {
        const directive = new SidebarGroupContentDirective();
        expect(directive).toBeTruthy();
    });
});
