import { SidebarGroupDirective } from "./sidebar-group.directive";

describe("SidebarGroupDirective", () => {
    it("should create an instance", () => {
        const directive = new SidebarGroupDirective();
        expect(directive).toBeTruthy();
    });
});
