import { SidebarMenuItemDirective } from "./sidebar-menu-item.directive";

describe("SidebarMenuItemDirective", () => {
    it("should create an instance", () => {
        const directive = new SidebarMenuItemDirective();
        expect(directive).toBeTruthy();
    });
});
