import { SidebarFooterDirective } from "./sidebar-footer.directive";

describe("SidebarFooterDirective", () => {
    it("should create an instance", () => {
        const directive = new SidebarFooterDirective();
        expect(directive).toBeTruthy();
    });
});
