# Combo Box

**Selector:** `mona-combo-box`

`ComboBoxComponent` is a text input that resolves typed or navigated text to a single selected item from a fixed collection, optionally allowing values that don't match any item.
