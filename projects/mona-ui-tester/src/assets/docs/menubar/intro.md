# Menubar

**Selectors:** `mona-menubar` · `mona-menu` · `mona-menu-item` · `mona-menu-checkbox-item` · `mona-menu-radio-group` · `mona-menu-radio-item` · `mona-menu-group` · `mona-menu-separator`

`MenubarComponent` renders a persistent, horizontal bar of top-level menus, each opening a popup menu built from content-projected menu item components.
