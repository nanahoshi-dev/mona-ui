# Breadcrumb

**Selector:** `mona-breadcrumb`

`BreadcrumbComponent` renders a navigational hierarchy trail as an accessible `<nav>` landmark with interactive, keyboard-operable items and an optional custom separator template.
