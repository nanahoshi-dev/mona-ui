# Multi Select

**Selector:** `mona-multi-select`

`MultiSelectComponent` is a combobox that displays selected values as removable tags and expands a listbox popup for the consumer to select any number of options from a fixed collection.
