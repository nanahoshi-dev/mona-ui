# Chip

**Selector:** `mona-chip`

`ChipComponent` renders an inline label element with optional toggle selection, removability, and a prefix template slot for use in tag inputs, filter lists, and selection collections.
