# Circular Progress Bar

**Selector:** `mona-circular-progress-bar`

`CircularProgressBarComponent` renders a circular SVG progress indicator with configurable value range, size, stroke thickness, color, and an optional custom label template.
