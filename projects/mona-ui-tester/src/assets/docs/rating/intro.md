# Rating

**Selector:** `mona-rating`

`RatingComponent` lets users choose a numeric value from a configurable visual scale of preset icons, with support for whole or half values, a configurable selected color, continuous or single-item selection, keyboard navigation, and custom templates.
