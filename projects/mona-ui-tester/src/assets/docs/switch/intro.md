# Switch

**Selector:** `mona-switch`

`SwitchComponent` renders a binary on/off toggle switch with optional state labels, a customizable sliding handle, and signal forms integration via `FormCheckboxControl`.
