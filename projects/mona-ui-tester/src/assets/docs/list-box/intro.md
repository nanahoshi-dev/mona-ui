# List Box

**Selector:** `mona-list-box`

`ListBoxComponent` renders a selectable list of items with an optional action toolbar for moving, clearing, removing, or transferring items to and from a second, connected list box.
