# Popover

**Selector:** `mona-popover`

`PopoverComponent` displays a floating panel — with an optional title, footer, and projected content — anchored to a target element and opened by a configurable trigger.
