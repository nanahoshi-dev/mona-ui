# Progress Bar

**Selector:** `mona-progress-bar`

`ProgressBarComponent` renders a horizontal progress indicator with a configurable value range, fill color, label, and an indeterminate animation for tasks whose completion is unknown.
