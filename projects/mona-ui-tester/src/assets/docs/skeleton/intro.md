# Skeleton

**Selector:** `mona-skeleton`

`SkeletonComponent` renders a decorative placeholder block for content that is still loading. Set its `width` and `height` to reserve the expected space, then replace it with the loaded content when it is available.
