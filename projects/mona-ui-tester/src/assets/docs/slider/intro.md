# Slider

**Selector:** `mona-slider`

`SliderComponent` is a single-handle range input that lets the user select a numeric value by dragging, clicking, or using keyboard navigation. It integrates with Angular signal forms via `FormValueControl<number>`.
