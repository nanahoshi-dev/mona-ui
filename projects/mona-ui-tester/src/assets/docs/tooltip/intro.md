# Tooltip

**Selector:** `mona-tooltip`

`TooltipComponent` shows a floating, positioned tooltip with projected content, anchored to a separately specified target element on pointer hover and keyboard focus.
