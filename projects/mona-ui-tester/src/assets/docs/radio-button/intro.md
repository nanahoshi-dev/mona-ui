# Radio Button

**Component selector:** `mona-radio-button` &nbsp;·&nbsp; **Directive selector:** `input[type='radio'][monaRadioButton]`

`RadioButtonComponent` renders a fully styled radio button with a managed label and `ControlValueAccessor` support. `RadioButtonDirective` applies the same visual style to a native `<input type="radio">` that consumers place and label themselves.
