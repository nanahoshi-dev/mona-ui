# Numeric Text Box

**Selector:** `mona-numeric-text-box`

`NumericTextBoxComponent` is a numeric input with optional spinner buttons, bounded values, configurable decimal formatting, and signal-forms support. When the field is focused, it shows the raw edit text; when it is not focused, it shows the formatted value.
