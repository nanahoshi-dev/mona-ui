# Color Gradient

**Selector:** `mona-color-gradient`

`ColorGradientComponent` is a color editor for selecting saturation, value, hue, and optional alpha, with hex/channel inputs and signal forms integration via `FormValueControl<string | null | undefined>`.
