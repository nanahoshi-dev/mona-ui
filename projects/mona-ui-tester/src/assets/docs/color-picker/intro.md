# Color Picker

**Selector:** `mona-color-picker`

`ColorPickerComponent` is a compact, combobox-style control that opens a popup containing either a color palette or a color gradient editor, with signal forms support via `FormValueControl<string | null>`.
