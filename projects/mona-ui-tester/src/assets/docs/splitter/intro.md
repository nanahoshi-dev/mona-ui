# Splitter

**Selectors:** `mona-splitter` · `mona-splitter-pane`

`SplitterComponent` lays out projected `SplitterPaneComponent` panes in a row or column and lets consumers resize them by dragging the bar between adjacent panes, with optional collapse/expand support per pane.
