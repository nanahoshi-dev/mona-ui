# Scroll View

**Selector:** `mona-scroll-view`

`ScrollViewComponent` renders a fixed-size carousel that pages through a data collection one item at a time, with optional navigation arrows, a dot pager, and animated slide transitions.
