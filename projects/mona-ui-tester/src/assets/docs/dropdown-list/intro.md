# Dropdown List

**Selector:** `mona-dropdown-list`

`DropdownListComponent` is a combobox that displays a single selected value and expands a listbox popup for the consumer to choose exactly one option from a fixed collection.
