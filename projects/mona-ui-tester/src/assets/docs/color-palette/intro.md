# Color Palette

**Selector:** `mona-color-palette`

`ColorPaletteComponent` renders a grid of selectable color swatches — a built-in flat, material, or websafe scheme, or a custom list of colors — with keyboard navigation and signal forms support via `FormValueControl<string | null>`.
