# Input Text

**Selector:** `input[monaTextBox]`

`TextBoxDirective` applies Mona UI input styles to a native `<input>` element, preserving all native browser behavior, form semantics, and keyboard interaction.
