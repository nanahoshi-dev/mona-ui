# Dialog

**Selector:** `mona-dialog`

`DialogComponent` and `DialogService` display a modal or non-modal message, confirmation, or content dialog with an optional icon, description, and action buttons.
