# Text Area

**Selector:** `textarea[monaTextArea]`

`TextAreaDirective` applies Mona UI styling to a native `<textarea>` element while preserving all native browser behavior — including resizing, keyboard access, and form submission.
