# List View

**Selector:** `mona-list-view`

`ListViewComponent` renders a collection of items as a listbox, with selection, keyboard navigation, grouping, pagination, and virtualization each opt-in through a companion directive.
