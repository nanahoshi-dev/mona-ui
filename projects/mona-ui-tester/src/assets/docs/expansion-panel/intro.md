# Expansion Panel

**Selector:** `mona-expansion-panel`

`ExpansionPanelComponent` is a disclosure component that shows a clickable header and hides its content until the panel is expanded — designed to stack vertically with adjacent panels sharing a seamless border.
