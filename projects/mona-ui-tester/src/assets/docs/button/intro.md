# Button

**Selector:** `button[monaButton]`

`ButtonDirective` applies Mona UI button styles and interactive behavior — loading state, toggle selection, disabled propagation, and ARIA attribute management — to a native `<button>` element.
