# Button Group

**Selector:** `mona-button-group`

`ButtonGroupComponent` wraps a set of `<button monaButton>` elements into a cohesive control that shares look, size, and border-radius across all child buttons and optionally coordinates toggle selection in single or multiple mode.
