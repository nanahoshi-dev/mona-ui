# Text Box

**Selector:** `mona-text-box`

`TextBoxComponent` is a styled text input wrapper that adds optional prefix/suffix template slots, a clear button, full Angular forms integration, and Mona UI theming, while allowing pass-through of arbitrary attributes to the inner `<input>`.
