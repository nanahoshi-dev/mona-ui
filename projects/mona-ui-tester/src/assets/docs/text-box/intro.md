# Text Box

**Selector:** `mona-text-box`

`TextBoxComponent` is a styled text input wrapper that provides optional prefix and suffix content slots, an optional clear button, signal forms integration via `[formField]`, and pass-through of arbitrary attributes to the inner `<input>`.
