# Range Slider

**Selector:** `mona-range-slider`

`RangeSliderComponent` is a two-handle range input that lets the user select a numeric interval by dragging either handle, clicking the track, or using keyboard navigation. It integrates with Angular signal forms via `FormValueControl<[number, number]>`.
